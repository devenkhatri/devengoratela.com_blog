import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devengoratela.blog',
  appName: 'devengoratela-com-blog',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
